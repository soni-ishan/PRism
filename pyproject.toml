[tool.pytest.ini_options]
markers = [
    "integration: marks tests that require real agent implementations (auto-skipped when agents are not ready)",
]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
